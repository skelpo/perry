[package]
name = "perry-diagnostics"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Diagnostic infrastructure for the Perry TypeScript compiler"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
