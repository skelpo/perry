/**
 * Module with sync functions for testing
 */
export function addStr(a: string, b: string): string {
  return a + b;
}

export function greetSync(name: string): string {
  return 'Hello, ' + name;
}
