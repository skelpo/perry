// Test that we can parse imports (they won't resolve yet, just testing parsing)
// For now just test that the file compiles even with unresolved imports

function main(): void {
    console.log(42);
}

main();
